export type Car = {
    id: number | null,
    make: string,
    model: string,
    year: number,
    price: number,
    vin: string,
    miles: number,
    color: string,
    isUsed: boolean,
    cleanTitle: boolean
}